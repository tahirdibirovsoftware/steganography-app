import { ProcessManager } from "@renderer/features/ProcessManager"

export const Processor = ():JSX.Element => {
    return (
        <ProcessManager color="#1890ff" size="6rem" message="Emal edilir..."/>
    )
}